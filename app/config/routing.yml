jobeet:
    resource: "@JobeetBundle/Controller/"
    type:     annotation
    prefix:   /

ens_jobeet:
    resource: "@EnsJobeetBundle/Controller/"
    type:     annotation
    prefix:   /

app:
    resource: "@AppBundle/Controller/"
    type:     annotation
